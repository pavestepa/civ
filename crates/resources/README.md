# civ-resources

Static game data registries (unit defs, building defs, tech trees).

## Note

Named `civ-resources` to avoid confusion with Bevy `Resource` trait.
Contains game design data, not ECS resources exclusively.
