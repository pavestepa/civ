use bevy::prelude::*;

/// Placeholder for GLTF scene loading. Uses Bevy asset pipeline only.
pub fn load_scene_assets(_asset_server: Res<AssetServer>) {
    // Future: asset_server.load("models/units/warrior.glb")
}
